# Core ML / embeddings
# NOTE: for CPU-only deployment (e.g. Streamlit Community Cloud, free-tier
# hosting), install torch from the CPU wheel index to avoid pulling in a
# large CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
open_clip_torch>=2.24

# Vector search
faiss-cpu>=1.8

# Image handling
Pillow>=10.0
numpy>=1.26

# Dataset download / indexing
requests>=2.31
tqdm>=4.66

# Agent framework + LLM
langchain>=0.3
langchain-core>=0.3
langgraph>=0.2
langchain-google-genai>=2.0
pydantic>=2.6
langchain-groq>=0.2

# Frontend
streamlit>=1.38
python-dotenv>=1.0

# Testing
pytest>=8.0
